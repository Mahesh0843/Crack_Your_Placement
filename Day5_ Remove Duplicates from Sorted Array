class Solution {
    public int removeDuplicates(int[] nums) {
        int i,j=0;
        int N=nums.length;
        for(i=1;i<N;i++)
        {
            if(nums[i]!=nums[j])
            {
                nums[j+1]=nums[i];
                j++;
            }
        }
        return j+1;
    }
}
